import 'package:flutter/material.dart';

class AddPicture extends StatelessWidget {
  const AddPicture({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Upload Image Screen')),
    );
  }
}